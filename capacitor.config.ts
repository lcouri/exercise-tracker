import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.exercisetracker.app',
  appName: 'exercise-tracker',
  webDir: 'dist'
};

export default config;
