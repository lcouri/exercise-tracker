#+TITLE: Todo
#+STARTUP: indent
#+TODO: TODO TODO_NEXT TODO_LATER DONE

* DONE Real reading and writing with dummy data / hand-written json
- just saving the 'next' section
- can edit notes as well, but something to implement after exercise view
** DONE Saving and loading of the exercise.json file working (kinda)
- decided to do the uncombining for now; I'd like to rethink how the schema works at some point, but this is fine for now
- put the save button in ExerciseItem for simplicity - will do something better later
- loading static exercise data initially because there might not be a saved exercise.json file around to read
- saves the whole exercise data (even 'notes')
- parsing for reading and especially writing data has a lot of casting 'as' - hopefully can do something about that later
- should probably do the same for programme.json, but that can come later - I'd rather get the exercise part to where I want it first, the programme part should be a lot simpler

* TODO_NEXT Module loading
Look into nice and typesafe ways of loading modules
- user saved data should say which modules are used for an exercise
- schemas should be able to dynamically add module schemas to the exercise schema
  - unless I can think of a better of handling exercise and module schemas
- ExerciseItem should be able to dynamically load and render a Summary component (in the item button that expands when you click it) and an Expanded component (in the expanded section of the exercise item)
  - these components are optional for a module to have
** DONE Module component to handle rendering in ExerciseItem
** TODO_NEXT Join module schema with exercise schema
** TODO Type safety in ModuleComponent
- accepted module names are essentially the names of folders inside @/modules/
  - probably have to manually update the type for each module added - I don't think it's possible for ts to make a type based on reading filesystem directories (without getting too weird at least)
    - could make a script to generate the type, but I don't see that as much better than just updating manually
- accepted component names are just ExerciseItemSummary and ExerciseItemExpanded for now
** TODO_LATER Think about handling loading and error states in ModuleComponent
- the components are optional, so it wouldn't know that a component failing to load is an error
  - unless there's something like a manifest - something to think about
** TODO_LATER Think about waiting for all the modules to load before rendering everything
- just in case there's any flickering or elements jumping around while modules are loading
- defineAsyncComponent takes a function that returns a Promise so a possible way is:
  - when a module component should be rendered/first used it registers that module with a loading state in a store somewhere
  - when the promise resolves, then change the loading state to loaded
  - when all are loaded (probably with a short delay in case modules are added after some have fully loaded), then that's the signal to render everything or remove the spinner

* TODO Modules
Split exercise data into plugins for:
** TODO sets + reps
** TODO weight
** TODO AMRAP
** TODO notes
** TODO then remove the whole combining/uncombining exercise data thing since it shouldn't be needed

* TODO Zod schemas for exercise & programme

* TODO Exercise View for adding/editing exercises
- should probably make my workout using this to make sure it works
  - tbh it's tempting to just directly write the json, but I'm thinking it would be annoying to do on the phone; I'd only do it on my laptop and I want this to be as easy to edit as possible
- would need to think about how to implement forms and saving
** Depends on Zod schemas

* TODO Programme View
- most of the hard work should be done before this, so it should be relatively simple
** Depends on Zod schemas

* TODO Temp file for keeping state when closing app
